name: Runner probe

on:
  workflow_dispatch:

jobs:
  linux:
    runs-on: ubuntu-latest
    steps:
      - run: echo "linux runner works"

  mac:
    runs-on: macos-26
    steps:
      - run: echo "macos runner works"
